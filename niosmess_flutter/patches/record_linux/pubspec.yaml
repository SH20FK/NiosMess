name: record_linux
version: 0.7.2
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  record_platform_interface: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
