name: record_linux
version: 0.7.2+nios
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  record_platform_interface: ^1.5.0

flutter:
  plugin:
    platforms:
      linux:
        pluginClass: RecordLinux
